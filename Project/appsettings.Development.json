/// <summary>
/// This is the development configuration file for the application.
/// It overrides certain settings from appsettings.json for development purposes.
/// </summary>

{
  "Logging": { // Development logging configuration
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  }
}
